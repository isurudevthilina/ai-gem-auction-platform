# Trained model artifacts are saved here by train.py
# gem_price_model.pkl — XGBoost model
# rmse_std.pkl        — Cross-validation RMSE std (used for confidence interval)
# feature_names.pkl   — Ordered list of feature column names
